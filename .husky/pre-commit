case "$(uname -s)" in
  MINGW*|MSYS*|CYGWIN*)
    if command -v chcp.com >/dev/null 2>&1; then
      chcp.com 65001 >/dev/null 2>&1
    fi
    export LANG=C.utf8
    export LC_ALL=C.utf8
    ;;
esac

npm run lint:staged
