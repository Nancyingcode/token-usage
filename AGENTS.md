# Agent 工作规范

## 1. 开发流程

- 设计文档和实施计划使用中文。
- 下列变更属于非简单变更：跨模块新增功能，修改公共数据模型、IPC 契约、安全边界或持久化格式，以及包含明显产品选择或架构选择的行为变更。非简单变更按以下流程执行：
  1. 先在 `docs/superpowers/specs/` 编写中文设计文档。
  2. 用户明确批准设计文档，或明确要求按已有设计实施后，视为设计已确认。
  3. 设计确认后，在 `docs/superpowers/plans/` 编写中文实施计划。
  4. 按计划拆分任务；包含行为代码的任务执行红—绿—重构循环。
- 普通 Bug 修复默认不要求设计文档与实施计划，但必须先添加能够复现问题的回归测试，再实现修复。若 Bug 修复涉及上述非简单变更，仍执行完整设计流程。
- 当问题无法通过自动化测试稳定复现时，应先记录可重复的验证步骤和无法自动化的原因，再实施修复并执行对应验证。
- 纯文档修改、简单重命名、不改变行为的小范围调整，以及不涉及产品或架构选择的局部实现调整，可以跳过设计文档与实施计划。
- 未确认设计前，不提前实现存在明显产品选择或架构选择的功能。

## 2. 验证要求

- 开发过程中优先运行受影响的单个测试文件或最小相关测试集。
- 声称代码或配置任务完成前，至少运行：
  - `npm test`
  - `npm run typecheck`
  - `npm run lint`
- 修改构建配置、Electron 主进程、preload、依赖或打包配置时，额外运行 `npm run build`。
- 纯文档修改不要求运行完整代码门禁，但必须检查 Markdown 结构、本地链接、UTF-8 编码和变更差异；若仓库提供文档检查脚本，应优先运行该脚本。
- 运行命令以 `package.json` 中的 scripts 为准。
- 测试、类型检查或 lint 失败时，不得声称已经完整验证。若验证因环境限制或与当前任务无关的既有失败而无法完成，应明确区分“实现完成”和“验证通过”，并列出未运行或失败的命令及根因。
- 修复失败时应定位并说明根因，不得通过删除测试、放宽有效断言、禁用规则或隐藏错误绕过验证。

## 3. 项目不变量

- Codex 会话目录包括默认目录和用户选择的 Token/会话数据源目录，始终按只读数据源处理。只允许读取、遍历、获取元数据和监听变化；不得在其中创建、写入、重命名、删除、修改权限或向网络上传数据。自动化测试必须使用仓库 fixture 或独立临时目录，不得操作真实会话目录。
- Renderer 不直接访问文件系统；文件访问必须由 Electron 主进程负责。
- 主进程与 Renderer 之间通过类型化的 IPC 和 preload API 通信。
- 未知模型价格不得猜测；必须保留 Token 数据并明确标记为未计价。
- 费用始终属于本地估算，不得描述为 OpenAI 实际账单。
- 核心计算优先实现为无副作用的纯函数，避免修改输入对象。

## 4. 代码与界面规范

### 4.1 通用规则

- 禁止使用 `any` 类型和 `var` 声明。
- 禁止硬编码业务魔法值。影响业务判断、费用、时间窗口、容量或告警等级且无法从语言语义直接推断的数字或字符串属于业务魔法值，必须使用具名常量或配置，并在名称不足以说明时标注单位、来源或规则原因。
- 不随意删除注释；确认注释已经失效、错误、重复实现细节或仅用于保留废弃代码后，删除前仍需取得用户确认。
- 核心业务逻辑是指费用与 Token 计算、定价覆盖、预算判断、会话解析、持久化迁移以及安全或 IPC 边界。相关注释应解释业务原因、不变量或边界条件，不复述代码实现。
- class 静态字段、模块级可变状态和长生命周期缓存必须通过注释说明生命周期、所有权及重置方式。名称与类型已完整表达含义的模块级只读常量无需重复注释；业务阈值仍按魔法值规则说明来源和单位。

### 4.2 React 与 TypeScript

- React 组件、Props、命名、渲染条件和状态建模遵循 [React 规范](./rules/react-guide.md)。

### 4.3 国际化与可访问性

- 新增或修改用户可见文案时，同时维护英文和简体中文资源。
- 禁止在 React 组件中硬编码用户可见文案。
- 国际化资源以 [`src/shared/i18n/resources.ts`](./src/shared/i18n/resources.ts) 为准，中英文 namespace 与叶子 key 必须保持一致，并由 [`tests/i18n.test.ts`](./tests/i18n.test.ts) 验证。
- 金额、百分比、数字和日期优先使用 [`src/renderer/utils/formatters.ts`](./src/renderer/utils/formatters.ts) 中的 locale formatter；新增格式化能力时同步维护 [`tests/formatters.test.tsx`](./tests/formatters.test.tsx)。仅在通用 formatter 无法表达组件语义时局部使用 `Intl`，并说明原因或补充专用纯函数测试。
- 交互控件必须支持键盘操作并具有可访问名称；优先使用语义化 HTML，无法使用时补充正确的 role、状态和键盘语义。
- Dialog、Drawer、菜单等覆盖层必须管理初始焦点、焦点约束和关闭后的焦点恢复；异步状态、错误和通知应向辅助技术提供可感知反馈。
- 动画必须遵循 `prefers-reduced-motion`；图表和仅悬停显示的信息必须提供键盘或静态文本等价入口。
- 颜色不得作为状态、严重程度或置信度的唯一表达方式。

## 5. 规则文档与自动化

- 根 `AGENTS.md` 只维护长期有效的项目原则和规则入口，不复制大段语法示例。
- JavaScript/TypeScript 风格以 ESLint、Prettier 和 TypeScript 配置的自动检查结果为准。
- 项目补充代码规范参见 [Coding Standards](./rules/style-guide.md)。
- 注释规范参见 [注释规范](./rules/annotation-guide.md)。
- 文件头规范参见 [文件头规范](./rules/file-header.md)。
- 本文件适用于仓库中的源码、测试、脚本、配置和文档；生成文件仅遵循其生成器与“禁止手工修改”约束。更深层目录存在 `AGENTS.md` 时可以补充局部规则，但不得放宽项目不变量和安全边界。
- 规则冲突时依次遵循：项目不变量和安全边界、本文件中的流程与行为规则、自动化配置中的机械规则、专项规则文档、非约束性参考资料。发现文档与自动化配置不一致时，应按该顺序执行并同步修正规则，不得长期保留冲突。
- Markdown 文档通过 `npm run lint:docs` 检查 UTF-8 编码、代码围栏和本地链接；`npm run lint` 必须包含该检查，提交阶段由 lint-staged 对变更的 Markdown 文件执行同一脚本。
- 能通过 lint、类型检查或测试自动执行的规则，应优先添加自动化检查，避免仅依赖文字约束。

## 6. Git 与变更范围

- 提交信息遵循 Conventional Commits。
- 每个提交只包含一个可独立解释的任务，且只包含该任务涉及的文件。
- 保留工作区中与当前任务无关的用户修改，不覆盖、不回滚、不顺带格式化无关文件。
- 除非用户明确要求，或正在执行已确认且要求逐任务提交的实施计划，否则不主动创建提交。
- 未经用户明确要求，不执行 push、创建 Pull Request、变基或破坏性 Git 操作。
