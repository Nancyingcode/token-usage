# 网络价格目录第一阶段实施计划

依据：[已确认设计](../specs/2026-09-10-network-pricing-design.md)。不创建提交或推送。

1. 先编写目录校验、优先级及采集解析测试，确认失败，再实现公共目录契约与采集脚本。
2. 先编写缓存、网络与刷新服务测试，确认失败，再实现主进程服务和持久化。
3. 先编写预算目录切换测试，确认失败，再连接预算运行时、应用启动和类型化 IPC。
4. 先编写价格同步界面测试，确认失败，再接入界面与中英文文案。
5. 添加定时发布工作流与运维文档，执行完整测试、类型检查、lint、构建及差异检查。

## 发布边界

托管目标为当前仓库 pricing 分支。启用远程服务需要用户后续将源码和工作流推送到默认分支并运行发布任务；本地实现期间 404 应显示更新失败并继续使用本地目录，不伪报已上线。

## 验证记录

- 本地实现已完成，未提交、未推送。
- npm test：132 个测试文件、699 个测试通过。
- npm run typecheck、npm run lint、npm run build、npm run pricing:validate 均通过。
- npm run pricing:update：官方 models.md 返回 HTTP 403，任务正常失败且未覆盖种子目录。真实官方 Markdown 结构解析和远程发布尚未完成验证。
- 采集器目前用明确格式 fixture 验证；首次部署仍需确认官方真实响应与该格式一致，若不一致须修复解析器后发布。
